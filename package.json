{
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd client && yarn install --only=dev && yarn install && yarn run build"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "express": "^4.16.3",
    "mongoose": "^5.3.2"
  },
  "engines": {
    "node": "8.10.0"
}
}
