import React from "react";
import "./style.css";

export const ProjectList = props => (
    <div>

        <div className="grid-container">
                {props.children}
            </div>
        
    </div>
);
